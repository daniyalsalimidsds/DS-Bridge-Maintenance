# گزارش کنترل نسخهٔ ۱.۶.۰

این گزارش به APK امضاشده با SHA-256 زیر مربوط است:

`18896e2b5b1e7a50e110cc953170d58d18076538e59e9c04bb758c551522e1e4`

مبنای ساخت: commit `080656b25b5efa971541d966acc792693f4ee187`، [اجرای CI شماره 34063041622](https://github.com/daniyalsalimidsds/DS-Bridge-Maintenance/actions/runs/34063041622)، تاریخ ۶ سپتامبر ۲۰۲۶. هویت بسته `ir.bridge.maintenance`، نسخه ۱.۶.۰، کد ۱۰۶۰۰، حداقل API 26 و هدف API 35 است.

| کنترل | نتیجه و دامنه |
|---|---|
| JavaScript | ۲۳ ورودی آزمون موفق؛ شامل امتیاز نمونه −۲۴۰، پوشش، سازگاری داده، GPS و رخداد |
| Java | ۶ آزمون واحد موفق |
| Android | ۲۵ آزمون موفق، بدون شکست؛ زمان ۱۶۷٫۴۶۹ ثانیه روی API 30 / Pixel 2 |
| ساخت | release و کنترل سورس موفق؛ ۳۸ هشدار lint و صفر Error/Fatal |
| چک‌لیست | پنج وضعیت، نمایش کامل نام بلند، هویت پایدار رخداد پس از حذف |
| ذخیره و امضا | رد نهایی‌سازی ناقص، الزام شواهد موارد استثنایی و تراکنش پایگاه داده |
| رابط | تصاویر WebView برای روز/شب، متن بزرگ و تازه‌سازی امتیاز پس از بازگشت |
| PDF | ۱۸۰ سطر توضیح ساختگی، ۸ صفحه، حفظ انتهای متن و جمع نهایی؛ جهت عدد منفی و GPS |
| Excel | سلول عددی −۴۰۰، سطر ادامه، ارتفاع کمتر از سقف Excel و حذف ستون خالی |
| امضا | APK Signature v2 و v3 معتبر؛ گواهی با فایل ۱.۵.۰ ارسالی یکسان |
| بسته نصب | SHA-256 مشخص، کنترل ZIP و نبود کلید خصوصی در بسته |

اثر انگشت گواهی امضا:

`19869b24c62aa922e9570337c3033e5c4cff0734d87816b69f9636dfa0e773b6`

نمونه‌های پوشه QA داخل بسته نصب ساختگی‌اند؛ گزارش بازرسی یک پل واقعی نیستند. تصاویر منتخب در [screenshots](screenshots/) و نتایج ماشینی در [شناسنامهٔ انتشار](../releases/v1.6.0/RELEASE_VERIFICATION.json) نگهداری می‌شوند.

## کنترل باقی‌مانده در محیط بهره‌برداری

آزمون روی گوشی واقعی، GPS میدانی، دوربین و نصب ارتقا روی دستگاه مقصد انجام نشده است. مالک باید واژگان و ضرایب تکمیلی پیشنهادی را برای کاربرد رسمی بپذیرد. موفقیت آزمون نرم‌افزار، تأیید ظرفیت باربری پل یا مصوبه یک روش مهندسی نیست.

اجرای خودکار انتشار، فایل‌ها را دوباره از GitHub Release دریافت و SHA-256 آن‌ها را با نسخهٔ محلی تطبیق می‌دهد. نسخهٔ منتشرشده با همان امضا حفظ می‌شود و در گردش انتشار دوباره کامپایل یا امضا نمی‌شود.
