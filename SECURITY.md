# امنیت و گزارش آسیب‌پذیری

کنترل‌های موجود و حدود آن‌ها در [امنیت و حریم داده](docs/SECURITY.md) توضیح داده شده‌اند.

اگر امکان گزارش خصوصی آسیب‌پذیری در زبانه Security فعال است، از همان مسیر استفاده کنید؛ در غیر این صورت موضوع را از راه ارتباط خصوصی موجود با مالک مخزن مطرح کنید. تا بررسی مشکل، دادهٔ حساس یا روش سوءاستفاده از اطلاعات واقعی را در Issue عمومی ننویسید.

نسخه، مدل Android، مراحل بازتولید با دادهٔ ساختگی و اثر مشاهده‌شده را ارسال کنید. فایل پشتیبان واقعی، اطلاعات مکانی حساس، امضای اشخاص و کلید/رمز امضای نرم‌افزار نباید در گزارش قرار بگیرد.

فایل‌های نصب را از Release همین مخزن دریافت و با SHA256SUMS کنترل کنید. اصالت امضای APK و هویت برنامه در شناسنامهٔ انتشار ثبت شده است.
